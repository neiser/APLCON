#include "KinFit.h"

extern "C" {
#include "APLCON_wrapper.h"
}





void KinFit::AddMeasuredVariable(const std::string &name, const double value, const double sigma, 
                                 const KinFit::VarProperty_t property, const double lowerLimit, const double upperLimit)
{
  
}
